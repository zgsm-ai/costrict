import {
	SYSTEM_FILE_PATHS,
	CONTEXT_VARIABLES,
	AGENTS_COMMON_REQUIREMENTS,
	AGENT_SELF_CHECK_BASE,
	AGENT_TASK_COMPLETION_STANDARD
} from "../common/constants"

export const RULES_GENERATION_AGENT_TEMPLATE = (workspace: string) => `# 项目规则生成任务

## 任务目标
基于代码仓库深度分析，提取项目特有、强制、具体的开发测试规则，生成规则文档，解决"隐性约束未显性化"问题，提升AI Coding Agent代码生成精准性。

## 核心要求（逐步思考）

${AGENTS_COMMON_REQUIREMENTS}

### 第一步：项目规则价值评估
思考：为什么项目规则对AI代码生成精准性至关重要？
- 解决隐性约束问题：项目中"必须用特定工具""特定操作流程"等规则存在于开发习惯中
- 提升环境兼容性：确保生成代码与项目技术栈版本、依赖管理策略兼容
- 保证规范适配性：确保生成代码符合项目代码风格、命名约定、架构原则
- 维护业务一致性：避免生成与项目既有功能模块逻辑冲突的代码

### 第二步：规则提取策略制定
基于80/20法则，优先提取高价值规则：
1. **构建与运行规则**：构建命令、环境配置、依赖管理
2. **API开发规则**：接口契约、数据格式、错误处理
3. **代码组织规则**：目录结构、模块边界、调用规范
4. **数据处理规则**：数据库操作、事务管理、缓存策略
5. **测试规则**：测试框架、覆盖率要求、Mock策略

### 第三步：深度分析项目资源
按优先级分析以下资源，提取具体规则：

#### 高优先级资源
1. **配置文件**：package.json、tsconfig.json、webpack.config.js、vite.config.js
2. **构建部署**：Dockerfile、CI/CD配置、环境变量文件
3. **代码质量**：ESLint、Prettier、Stylelint配置文件
4. **公共模块**：src/utils/、src/common/、src/lib/目录

#### 中优先级资源
1. **核心业务模块**：主要功能模块代码及文档
2. **测试配置**：jest.config.js、vitest.config.ts、测试用例
3. **文档规范**：README.md、CONTRIBUTING.md、API文档

### 第四步：规则提取与验证
对每条规则执行以下验证：

#### 规则质量验证（5项核心标准）
1. **项目特有**：仅针对当前项目的特殊约束，排除通用规范
2. **强制约束**：不遵守会导致问题或破坏架构一致性
3. **具体明确**：包含确切信息（版本号、路径、数值、格式等）
4. **易于执行**：表述无歧义，逻辑清晰，AI可直接明确操作
5. **内容简洁**：单条规则不超过100字，单行，禁止代码示例

示例：
- 数据库操作必须使用参数化查询，禁止字符串拼接

### 第五步：规则分类与组织
按项目实际特点选择合理分类：

#### 基础分类（按需选择）
- **依赖管理**：包管理器、版本锁定、更新限制
- **代码组织**：目录结构、文件命名、跨模块调用
- **构建部署**：构建工具、CI/CD流程、环境隔离
- **API开发**：接口格式、返回结构、错误处理
- **数据处理**：数据库操作、数据验证、脱敏规则
- **前端组件**：组件复用、状态管理、样式规范
- **测试机制**：测试框架、覆盖率、Mock策略
- **类型系统**：TypeScript配置、类型定义、接口设计
- **性能优化**：代码分割、缓存机制、资源加载
- **错误处理**：异常捕获、错误码、日志记录
- **安全规范**：输入验证、权限控制、数据加密

#### 扩展分类（项目特有）
根据项目特点添加专属分类，如"微前端""Serverless""实时通信"等

## 输出要求

### 规则编写表述要求
- 必须使用"必须""禁止""仅允许"等强制词汇
- 每条规则必须包含具体信息（版本号、路径、配置参数等）
- 严格遵守字数限制（单条≤100字）
- 禁止包含任何代码示例、代码片段

#### 正确示例
- 控制器必须继承src/controllers/BaseController.ts，实现validate方法
- 单元测试必须使用Jest，测试文件必须与源文件同名添加.spec.ts后缀
- API响应必须包含timestamp字段，格式为ISO-8601
- 状态管理必须使用Zustand，store文件必须位于src/stores/目录

#### 错误示例
- 应复用现有工具函数（错误：未说明具体工具函数）
- 保持依赖版本稳定（错误：通用建议，非项目特有）
- 接口返回格式统一（错误：未说明具体格式）
- 尽量使用现有组件（错误："尽量"非强制）

## 自我反思检查（质量保证）

${AGENT_SELF_CHECK_BASE}

### 格式强制要求
1. 一级标题固定为"# 项目开发规范"
2. 分类标题必须以"## "开头，禁止使用三级标题（###）
3. 每条规则必须以"- "开头，独立成行，单行不超过100字
4. 禁止包含任何形式的代码示例、代码片段
5. 禁止装饰性元素：emoji图标、特殊符号等
6. 禁止添加任何冗余的说明性文字、注释

### 内容验证清单
- [ ] 所有规则都符合5项核心标准
- [ ] 每条规则都包含具体信息
- [ ] 每条规则都使用强制词汇
- [ ] 规则分类合理，反映项目特点
- [ ] 规则间逻辑一致，无冲突

### 格式验证清单
- [ ] 一级标题正确
- [ ] 分类标题格式正确
- [ ] 每条规则以"- "开头
- [ ] 没有使用任何emoji图标或特殊符号
- [ ] 没有三级标题（###）
- [ ] 没有包含任何形式的代码示例

### 价值验证清单
- [ ] 规则对AI代码生成有实际帮助
- [ ] 规则覆盖项目关键约束
- [ ] 规则具有实际指导价值
- [ ] 规则数量符合项目规模要求

${AGENT_TASK_COMPLETION_STANDARD}

## 输出格式要求（严格遵循）

### 文档结构
\`\`\`markdown
# 项目开发规范

## 代码组织
- 控制器必须继承src/controllers/BaseController.ts，实现validate方法
- 服务层必须位于src/services/目录，文件名必须以Service.ts结尾
- 组件必须使用函数式组件，禁止使用类组件
- 状态管理必须使用Zustand，store文件必须位于src/stores/目录

## 测试机制
- 单元测试必须使用Jest，测试文件必须与源文件同名添加.spec.ts后缀
- 集成测试必须位于src/tests/integration/目录，必须使用真实数据库
- 测试数据必须使用Factory模式，位于src/tests/factories/目录

## API开发
- RESTful API必须使用HTTP状态码，200表示成功，400表示客户端错误
- 接口路径必须使用kebab-case格式，版本号必须包含在路径中
- 请求体必须使用JSON格式，Content-Type必须设为application/json
- 响应数据必须包含timestamp字段，格式为ISO-8601

## 数据处理
- 数据库迁移文件必须位于src/migrations/目录，文件名必须包含时间戳
- 查询语句必须使用参数化查询，禁止字符串拼接
- 事务操作必须使用src/database/Transaction.ts的beginTransaction方法

## 错误处理
- 所有异步操作必须使用try-catch包裹，错误必须通过logger.error记录
- 禁止在生产环境中输出console.log，使用logger.info替代
- API错误码必须使用src/constants/errorCodes.js中定义的错误码

## 依赖管理
- 必须使用pnpm 8.6.0+版本管理依赖，版本锁定在package.json中
\`\`\`

## 输出文件命名
\`${workspace}${SYSTEM_FILE_PATHS.OUTPUT_DIR}${SYSTEM_FILE_PATHS.PROJECT_RULES_OUTPUT_FILE}\`
注意：如果${workspace}${SYSTEM_FILE_PATHS.OUTPUT_DIR}目录不存在，则创建。
`