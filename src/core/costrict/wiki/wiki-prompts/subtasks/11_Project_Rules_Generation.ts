import { GENERAL_RULES_OUTPUT_DIR, WIKI_OUTPUT_DIR, SUBTASK_OUTPUT_FILENAMES } from "./constants"

export const PROJECT_RULES_GENERATION_TEMPLATE = `# 🎯 项目开发规则文档生成任务

你是一个专业的项目规则分析师，需要基于项目代码仓库分析生成项目特异性的开发规则文档。

## 🎯 核心指令（必须遵守）

### 任务目标
生成**项目特有的、强制性的、具体的**开发规则，专注于：
- 不遵守会导致bug、系统崩溃、运行失败的关键约束
- 维护项目架构一致性的强制要求
- 项目特定的技术栈配置和版本限制

### 输入资源
- **文档索引**：${WIKI_OUTPUT_DIR}${SUBTASK_OUTPUT_FILENAMES.INDEX_GENERATION_TASK_FILE}
- **技术文档**：${WIKI_OUTPUT_DIR} 目录下的所有分析文档
- **代码仓库**：项目核心文件（最多30个）

### 强制性要求
1. **表述方式**：每条规则必须使用"必须"、"禁止"、"严格要求"、"建议"等明确词汇
2. **具体程度**：包含确切的版本号、路径、数值、格式等具体参数
3. **长度控制**：单条规则不超过60字，整个文档不超过150行
4. **格式规范**：仅使用一级标题(#)和二级标题(##)，禁止三级及以下标题

### 禁止内容
- ❌ 抽象原则："保持代码简洁"、"遵循最佳实践"
- ❌ 通用建议："使用有意义的变量名"、"编写清晰的注释"
- ❌ 模糊描述："适当优化性能"、"合理使用缓存"
- ❌ 代码示例：任何形式的代码片段或配置示例
- ❌ 复杂格式：表格、列表嵌套、多级标题

## 📝 指导示例（参考执行）

### ✅ 正确示例（具体、可执行）
- 必须使用React 18.2.0版本，禁止升级到19.x系列
- API响应时间不得超过500ms，超时必须返回504状态码
- 必须使用pnpm作为包管理器，版本要求8.6.0以上
- 环境变量必须以ZGSM_前缀命名，如ZGSM_API_KEY

### ❌ 错误示例（抽象、模糊）
- 使用稳定版本的React框架
- API响应时间要尽可能快
- 选择合适的包管理器
- 环境变量命名要规范

## 🏗️ 分类选择指南（根据项目实际情况选择）

### 第一步：识别项目技术栈
根据项目代码分析，确定项目使用的主要技术栈，然后选择对应的分类：

**前端项目重点关注：**
- 前端框架规则（React/Vue/Angular版本、配置）
- 构建工具规则（Webpack/Vite配置、优化策略）
- 包管理规则（npm/yarn/pnpm版本、依赖锁定）

**后端项目重点关注：**
- 后端框架规则（Node.js/Python/Java框架版本）
- 数据库规则（MySQL/PostgreSQL/MongoDB配置）
- API设计规则（RESTful/GraphQL规范）

**全栈项目重点关注：**
- 前后端集成规则
- 数据交互规则
- 统一配置管理规则

### 第二步：选择核心分类
从以下分类中选择3-6个最适合项目的分类，按重要性排序：

**基础架构类：**
- 依赖管理：包管理器选择、版本锁定、依赖更新策略
- 代码组织：目录结构、命名规范、模块化要求
- 构建部署：构建工具配置、CI/CD流程、容器化要求

**功能开发类：**
- API开发：接口设计、数据格式、错误处理
- 数据处理：数据验证、数据库操作、数据迁移
- 前端组件：组件规范、状态管理、样式要求

**质量保障类：**
- 测试要求：测试框架、覆盖率、测试数据管理
- 安全合规：数据安全、网络安全、合规要求
- 性能监控：性能指标、监控告警、资源管理

### 第三步：根据项目特点扩展
**如果项目有特殊需求，添加专门分类：**

**技术栈特定：**
- 微前端架构：子应用隔离、共享依赖、通信机制
- Serverless：函数配置、冷启动优化、资源限制
- 区块链：智能合约规范、节点配置、交易处理

**业务领域特定：**
- 实时通信：WebSocket配置、消息队列、连接管理
- 大数据处理：数据管道、批处理配置、性能优化
- AI/ML：模型版本、数据处理、训练配置

### 第四步：确定规则数量
- 小型项目（<10k行代码）：每个分类2-4条规则，总计10-20条
- 中型项目（10k-50k行代码）：每个分类3-6条规则，总计20-40条
- 大型项目（>50k行代码）：每个分类4-8条规则，总计30-60条

**重要原则：**
- 优先选择与项目实际相关的分类
- 每个分类下的规则必须是项目特有的、具体的、强制性的
- 不相关的分类直接跳过，不需要包含
- 可以根据项目需要创建新的分类名称

## 📋 输出格式模板

按照以下markdown格式输出文档，根据项目实际情况调整分类和规则：

\`\`\`markdown
# {项目名称} 开发规范

## {选择的分类1名称}
- {具体规则1，必须包含具体数值、版本或路径}
- {具体规则2，必须使用"必须"、"禁止"等词汇}
- {具体规则3，针对项目特有约束}

## {选择的分类2名称}
- {具体规则1，必须包含具体数值、版本或路径}
- {具体规则2，必须使用"必须"、"禁止"等词汇}
- {具体规则3，针对项目特有约束}

## {选择的分类3名称}
- {具体规则1，必须包含具体数值、版本或路径}
- {具体规则2，必须使用"必须"、"禁止"等词汇}
- {具体规则3，针对项目特有约束}

{根据项目需要添加更多分类}
\`\`\`

**输出要求：**
- 分类名称根据项目实际情况确定，不要使用模板中的占位符
- 每个分类下的规则数量根据项目复杂度调整
- 所有规则必须基于项目实际分析，不要照搬示例

## 🔍 质量验证（生成后检查）

### 自检清单
生成规则文档后，逐一检查以下项目：
- [ ] **具体性检查**：每条规则包含具体的数值、路径、版本或格式要求
- [ ] **强制性检查**：使用"必须"、"禁止"、"严格要求"、"建议"等明确词汇
- [ ] **长度检查**：单条规则≤60字，文档总长度≤150行
- [ ] **格式检查**：仅使用一级(#)和二级(##)标题，无复杂格式
- [ ] **内容检查**：无代码片段、无抽象原则、无通用建议
- [ ] **项目特异性检查**：所有规则都是项目特有的，非通用最佳实践

### 优化迭代
如果发现不符合要求的规则，按以下优先级处理：
1. **优先保证具体性**：确保每条规则都有具体参数
2. **其次保证强制性**：使用明确的强制性词汇
3. **然后保证项目特异性**：删除通用性规则
4. **最后调整格式**：确保格式符合要求

### 冲突处理
当规则之间出现冲突时：
- **具体性 > 格式要求**：宁可超出字数限制也要保证具体性
- **项目特异性 > 通用规则**：项目特有规则优先于通用建议
- **强制性 > 建议性**："必须"类规则优先于"建议"类规则

## 📁 输出文件路径
\`${GENERAL_RULES_OUTPUT_DIR}${SUBTASK_OUTPUT_FILENAMES.PROJECT_RULES_TASK_FILE}\`
`
