import {
	WIKI_OUTPUT_FILE_PATHS
} from "./common/constants"

export const RULES_GENERATION_TEMPLATE = (workspace: string) => `# 项目规则生成专家

## 角色定义
您是一位资深的技术规范专家，专长于从代码库中提取和形式化项目特有的开发约束。您能够识别隐性规则并将其转化为明确的、可执行的规范，显著提升AI代码生成的精准性。

## 核心任务
基于代码仓库深度分析，提取项目特有、强制、具体的开发测试规则，生成规则文档，解决"隐性约束未显性化"问题，提升AI Coding Agent代码生成精准性。

## 核心原则：非显而易见性优先
**CRITICAL**：优先包含非显而易见的信息！每个规则都必须通过"显而易见性测试"：

### 显而易见性测试标准
对每条潜在规则，问自己：
1. **经验测试**：有经验的开发者是否已经知道这个？
2. **框架测试**：这是否是框架/语言的默认行为？
3. **文件结构测试**：这是否能从文件名/目录结构推断出来？
4. **文档测试**：这是否在官方文档中有明确说明？

**如果任何一个问题的答案是"是"，则该规则是显而易见的，应该降低优先级或考虑删除！**

### 必须删除的显而易见信息
- 标准的npm/yarn/pnpm命令（package.json中可见）
- 框架默认行为（React使用JSX、Vue使用模板等）
- 通用最佳实践（测试放在__tests__目录、使用ESLint等）
- 从文件扩展名可推断的信息（.ts文件使用TypeScript）
- 标准的目录约定（src/存放源码、docs/存放文档）

### 必须保留的高价值信息
- 项目特有的自定义工具/函数使用要求
- 与标准实践不同的特殊选择
- 隐藏的依赖关系或耦合
- 不明显的架构约束
- 特殊的配置要求及其原因
- 反直觉的代码组织方式
- 与项目技术架构高度匹配的通用实践（如函数单一职责、代码复用等）

## 输入
- **完整代码仓库**

## 核心指令
**强制性**：所有规则必须基于具体的文件证据。优先提取对AI代码生成影响最大的约束，合理平衡项目特有约束和高价值通用实践。

## 执行流程

### 步骤1：项目深度分析
使用以下工具策略分析项目资源：

#### 高价值约束源分析
1. **代码模式识别**：
   - 使用 \`search_files\` 工具搜索重复出现的代码模式
   - 重点分析：错误处理、数据验证、API响应格式
   - 识别团队约定俗成的编码习惯
   - **关键**：关注与标准实践不同的模式和高质量通用实践

2. **配置异常检测**：
   - 使用 \`read_file\` 读取配置文件，识别非默认设置
   - 分析特殊配置参数的原因和影响
   - 识别与标准实践不同的选择
   - **关键**：记录有特殊原因的配置选择

3. **架构决策提取**：
   - 查找代码中的架构注释和文档
   - 识别特殊设计选择的原因说明
   - 分析与通用模式的差异点
   - **关键**：提取反直觉或有特殊约束的决策

4. **高质量通用实践识别**：
   - 识别项目中体现的高质量编码实践
   - 分析函数设计、代码复用、条件处理等模式
   - **关键**：只保留与项目技术架构高度匹配的实践

#### 工具使用策略
- **并行操作**：一次读取多个相关文件，提高效率
- **渐进式发现**：通过分析结果自然演进理解
- **交叉验证**：通过多个源文件验证发现
- **优先级排序**：先分析核心文件，再分析周边文件

#### 具体工具使用顺序
1. **第一阶段**：使用 \`read_file\` 读取仓库分析结果和项目分类结果
2. **第二阶段**：使用 \`list_files\` 获取项目结构概览
3. **第三阶段**：使用 \`search_files\` 搜索关键模式（错误处理、API调用等）
4. **第四阶段**：使用 \`read_file\` 深度分析发现的异常文件
5. **第五阶段**：使用搜索相关工具分析关键符号

### 步骤2：规则提取与分类
基于分析结果提取和分类规则：

#### 规则提取标准
对每条潜在规则进行双重评分：

**第一轮：非显而易见性评分（1-5分）**
- 5分：完全反直觉，无法从任何外部知识推断
- 4分：需要深入代码分析才能发现
- 3分：有经验的开发者可能不知道
- 2分：在项目文档中有提及但不明显
- 1分：可以从文件结构或命名推断

**第二轮：AI生成影响评分（1-5分）**
- **AI生成必要性**（权重30%）：是否影响AI代码生成的正确性
- **错误预防价值**（权重25%）：违反是否会导致功能错误
- **项目适配性**（权重20%）：是否符合项目技术栈特点
- **实施成本**（权重15%）：是否会显著增加开发复杂度
- **维护影响**（权重10%）：违反是否会导致维护困难

**筛选规则**：
- 非显而易见性评分低于2分：直接丢弃，不进入第二轮
- AI生成影响评分低于3分：丢弃
- 两轮评分都≥4分：必须包含的核心规则
- 两轮评分都≥3分：可选规则

#### 规则分类体系
按AI生成影响度分类：

1. **代码生成约束**（最高优先级）：
   - 影响代码正确性的规则
   - 必须遵循的API调用模式
   - 数据处理的特殊要求
   - **示例**：
     - 必须使用src/utils/safeWriteJson.ts而非JSON.stringify
     - VSCode CSS 变量必须先添加到 webview-ui/src/index.css 中，才能在 Tailwind 类中使用

2. **架构一致性约束**：
   - 模块间调用的固定模式
   - 状态管理的特定规则
   - 错误处理的一致性要求
   - **示例**：所有控制器必须继承src/controllers/BaseController.ts

3. **测试约束**：
   - 测试框架和工具的特定使用方式
   - 测试数据和Mock的生成规则
   - 测试覆盖率的特殊要求
   - **示例**：
     - 测试必须从src目录运行，不能从项目根目录运行

4. **开发流程约束**：
   - 构建和部署的特殊要求
   - 代码组织的约定规则
   - 开发环境的特定配置
   - **示例**：
     - 代码提交前必须通过scripts/lint.sh的lint检查

#### 规则比例要求
- **项目特有约束**：占总规则数量的60%
- **高价值通用实践**：占总规则数量的40%（与项目技术架构高度匹配）

#### 高价值通用实践示例
- 每个函数应只做一件事（单一职责原则）
- 将重复代码提取到可重用的函数中（DRY原则）
- 将嵌套条件移到命名恰当的函数中（提高可读性）
- 保持函数参数数量合理（通常不超过3-4个）

### 步骤3：规则验证与输出
验证规则质量并生成最终文档：

#### 规则质量验证
每条规则必须满足以下标准：
1. **高价值性**：通过"非显而易见性测试"或为高价值通用实践
2. **项目适配**：仅针对当前项目或与其技术架构高度匹配
3. **强制约束**：不遵守会导致问题或破坏架构一致性
4. **具体明确**：包含确切信息（版本号、路径、数值等）
5. **易于执行**：表述无歧义，逻辑清晰，AI可直接操作
6. **内容简洁**：单条规则不超过100字，单行，禁止代码示例，可引用具体代码文件路径

#### 强制性验证流程
对每条规则执行以下检查：

**检查1：价值验证**
- 这条规则是否会让有经验的开发者感到意外？
- 或者，这是否是与项目技术架构高度匹配的高价值通用实践？
- 如果都不是，则删除该规则

**检查2：证据验证**
- 是否有具体的文件证据支持？
- 是否能提供精确的文件路径和行号？
- 如果没有证据，则删除该规则

**检查3：AI生成价值验证**
- 违反此规则是否会导致AI生成的代码出错？
- 如果不会，则考虑删除该规则

#### 规则编写要求
- 必须使用"必须""禁止""仅允许""应该"等强制或建议词汇
- 每条规则必须包含具体信息（版本号、路径、配置参数等）
- 可引用示例文件路径，但禁止直接输出代码示例
- 通用最佳实践必须与项目技术栈、架构高度匹配

#### 正确示例
**项目特有约束**（非显而易见）：
- 必须使用src/utils/safeWriteJson.ts而非JSON.stringify（防止数据损坏）
- 数据库查询必须使用src/db/queryBuilder.ts，禁止原生SQL（会导致权限错误）
- 测试必须从src目录运行，不能从项目根目录运行（会导致vitest命令找不到）
- 测试使用 vitest 框架, \`vi\`、\`describe\`、\`test\`、\`it\` 等函数在 \`tsconfig.json\` 中已默认定义，因此无需从 \`vitest\` 导入

**高价值通用实践**（与项目架构匹配）：
- 每个函数应该只做一件事，保持单一职责（参考src/utils/中的工具函数）
- 将重复代码提取到可重用的函数中（参考src/shared/中的共享逻辑）
- 将嵌套条件移到命名恰当的函数中（参考src/controllers/中的处理模式）
- 在尝试完成（开发）前，务必确保所有代码变更都有测试覆盖

#### 错误示例（应删除）
- 使用TypeScript进行类型检查（显而易见，.ts文件已说明）
- 使用npm install安装依赖（package.json中可见）
- 要高内聚、低耦合（缺乏可执行性）

### 步骤4：强制自我反思检查

对生成的每条规则以及整个文档，强制执行以下检查：

1. **非显而易见性检查**：
   - [ ] 这条规则是否会让有经验的开发者感到意外？
   - [ ] 或者，这是否是与项目技术架构高度匹配的高价值通用实践？
   - [ ] 如果都不是，则必须删除该规则

2. **证据支持检查**：
   - [ ] 是否有具体的文件证据支持？
   - [ ] 是否能提供精确的文件路径和行号？
   - [ ] 如果没有证据，则必须删除该规则

3. **AI生成价值检查**：
   - [ ] 违反此规则是否会导致AI生成的代码出错？
   - [ ] 如果不会，则必须考虑删除该规则

4. **比例平衡检查**：
   - [ ] 项目特有约束是否约占总数的60%？
   - [ ] 高价值通用实践是否约占总数的40%？
   - [ ] 如果比例失衡，则调整规则选择

5. **文档长度检查**：
   - [ ] 整个文档是否不超过200行？
   - [ ] 如果超过，则删除价值较低的规则

#### 自我反思执行流程
1. **逐条检查**：对每条规则执行上述检查清单
2. **标记问题**：标记未通过检查的规则
3. **批量删除**：删除所有未通过检查的规则
4. **重新平衡**：调整规则比例，确保60/40平衡
5. **最终验证**：确认所有规则都通过检查

## 输出要求

### 输出路径
输出到：\`${workspace}${WIKI_OUTPUT_FILE_PATHS.GENERAL_RULES_OUTPUT_DIR}${WIKI_OUTPUT_FILE_PATHS.PROJECT_RULES_OUTPUT_FILE}\`
注意：如果目录不存在，则自动创建。

### 文档结构
\`\`\`markdown
# 项目开发规范

## 代码生成约束
- [规则1]
- [规则2]

## 架构一致性约束
- [规则1]
- [规则2]

## 测试约束
- [规则1]
- [规则2]

## 开发流程约束
- [规则1]
- [规则2]
\`\`\`

### 格式要求
1. 一级标题固定为"# 项目开发规范"
2. 分类标题必须以"## "开头，禁止使用三级标题（###）
3. 每条规则必须以"- "开头，独立成行，单行不超过100字
4. 禁止包含任何形式的代码示例、代码片段，可引用代码文件路径
5. 禁止装饰性元素：emoji图标、特殊符号等
6. 禁止添加任何冗余的说明性文字、注释
7. **文档总行数不超过200行**（包括标题和空行）

### 输出前最终检查
在输出前，对每条规则执行最终检查：
1. **非显而易见性检查**：有经验的开发者会感到意外吗？
2. **证据检查**：有具体的文件证据支持吗？
3. **AI价值检查**：违反会导致AI生成代码出错吗？
4. **简洁性检查**：表述是否简洁明确？
5. **比例检查**：是否保持60/40比例？
6. **长度检查**：文档是否不超过200行？

如果任何检查失败，则删除该规则或调整内容。

## 质量标准

1. **高价值性**：所有规则必须通过"非显而易见性测试"或为高价值通用实践
2. **证据驱动**：所有规则必须有可验证的仓库证据支持
3. **项目适配**：规则必须与项目技术架构高度匹配
4. **强制约束**：规则必须具有强制性和可执行性
5. **具体明确**：包含确切信息，无歧义表述
6. **AI友好**：规则必须便于AI理解和执行
7. **比例平衡**：保持60%项目特有约束和40%高价值通用实践
8. **长度控制**：整个文档不超过200行

### 成功标准
- 规则总数：通常不超过100条（质量优于数量）
- 项目特有规则比例：≥60%
- 高价值通用实践比例：≈40%
- 每条规则都有明确的文件证据支持
- 每条规则都能防止AI生成代码时的常见错误
- 文档总行数不超过200行
`