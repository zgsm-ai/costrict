# Task assignment for cs

Read `plans/2026-05-18-mcp-async-polling-v1a.md` and run **Task 13 — Full workspace verification**.

This is a verification-only task, no new commits unless something needs fixing.

## Steps

1. Run `pnpm check-types`. Report any errors.
2. Run `pnpm lint`. Report any errors.
3. Run the broader test suite for the V1a-modified surfaces, **without** invoking `pnpm test` (it triggers a long bundle):

    ```powershell
    pnpm --filter @roo-code/types exec vitest run
    pnpm --filter zgsm exec vitest run src/services/mcp/__tests__/McpHub.spec.ts
    pnpm --filter zgsm exec vitest run src/services/mcp/asyncPolling/
    pnpm --filter zgsm exec vitest run src/core/tools/__tests__/useMcpToolTool.spec.ts
    pnpm --filter @roo-code/vscode-webview exec vitest run src/components/chat/__tests__/McpExecution.spec.tsx
    ```

4. If any failure is observed, identify whether it's a `mcpExecutionStatus` discriminated-union exhaustiveness regression (Task 13 step 2 in the plan warns about this) — if so, fix only the minimally affected `switch` block(s) by adding `case "polling":` / `case "stopped_waiting":` returning `null` or the equivalent no-op, then re-run.

5. If a fix was needed, commit with `chore(mcp): V1a async polling cross-workspace verification`. Otherwise no commit.

## Required deliverables in your final response

- check-types result (clean / errors fixed)
- lint result (clean / errors fixed)
- Test result for each of the 5 vitest commands above
- Any commit hash from `git log -1 --oneline` (or "no follow-up commit needed")
